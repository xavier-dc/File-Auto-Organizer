This main.py will automatically sort files from the downloads folder into their respective folders that were pre-created in other directories.

This will help automatically loose organize files so that they are easier to sort manually
